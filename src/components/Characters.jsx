import React from "react";
import Busqueda from "./Busqueda";
import Pagina from "./pagina";

const Characters = ({ info,setBuscar,pageNumber,setpageNumber,characters, setCharacters }) => {
  const clearState = () => {
    setCharacters(null);
  };



  return (
  <></>
  );
};

export default Characters;